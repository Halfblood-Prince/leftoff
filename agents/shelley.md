# Shelley Adapter

Status: Supported
Installer aliases: `shelley`
Suggested target: `~/.shelley/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Shelley uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
