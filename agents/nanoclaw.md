# NanoClaw Adapter

Status: Supported
Installer aliases: `nanoclaw`
Suggested target: `~/.nanoclaw/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If NanoClaw uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
