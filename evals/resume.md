# /resume Evaluation

Fixture expectation:

- current branch, head, changed paths, and recent commits are reported when Git is available;
- stale saved branch/head/worktree differences are explicit;
- no repository degrades gracefully.
