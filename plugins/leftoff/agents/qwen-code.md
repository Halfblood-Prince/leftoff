# Qwen Code Adapter

Status: Experimental
Installer aliases: `qwen-code`, `qwen`
Suggested target: `~/.qwen-code/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Qwen Code uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
