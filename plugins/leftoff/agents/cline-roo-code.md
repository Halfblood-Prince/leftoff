# Cline / Roo Code Adapter

Status: Supported
Installer aliases: `cline-roo-code`, `cline`, `roo`, `roo-code`
Suggested target: `~/.cline/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Cline or Roo Code uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
