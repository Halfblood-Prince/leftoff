# Goose Adapter

Status: Experimental
Installer aliases: `goose`
Suggested target: `~/.goose/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Goose uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
