# Kiro CLI Adapter

Status: Experimental
Installer aliases: `kiro-cli`, `kiro`
Suggested target: `~/.kiro/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Kiro CLI uses a different local instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
