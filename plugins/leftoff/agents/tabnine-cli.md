# Tabnine CLI Adapter

Status: Experimental
Installer aliases: `tabnine-cli`, `tabnine`
Suggested target: `~/.tabnine/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Tabnine CLI uses a different local instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
