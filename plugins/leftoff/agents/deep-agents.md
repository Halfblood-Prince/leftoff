# Deep Agents Adapter

Status: Supported
Installer aliases: `deep-agents`, `deepagents`
Suggested target: `~/.deep-agents/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Deep Agents uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
