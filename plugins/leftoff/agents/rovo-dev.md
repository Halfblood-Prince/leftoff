# Rovo Dev Adapter

Status: Experimental
Installer aliases: `rovo-dev`, `rovo`
Suggested target: `~/.rovo/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Rovo Dev uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
