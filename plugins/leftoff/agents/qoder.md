# Qoder Adapter

Status: Experimental
Installer aliases: `qoder`
Suggested target: `~/.qoder/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Qoder uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
