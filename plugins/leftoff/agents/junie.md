# Junie Adapter

Status: Experimental
Installer aliases: `junie`
Suggested target: `~/.junie/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If Junie uses a workspace-specific instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
