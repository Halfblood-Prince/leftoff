# GitHub Copilot CLI Adapter

Status: Supported
Installer aliases: `github-copilot-cli`, `copilot-cli`, `copilot`
Suggested target: `~/.github-copilot-cli/skills/leftoff/`

Load `skills/leftoff/SKILL.md` as the canonical skill instruction. If GitHub Copilot CLI uses a different local instructions path, install with `--target <path>`.

Use the launcher commands from `skills/leftoff/SKILL.md` for deterministic local operations.
